// CardDescription
import QtQuick 2.15
import myproject 1.0

Text {
    id: root
    color: Theme.mutedForeground
    font.pixelSize: 14      // Matches "text-sm" in TSX
    lineHeight: 1.5
    wrapMode: Text.WordWrap
}
