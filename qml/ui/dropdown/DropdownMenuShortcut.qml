// DropdownMenuShortcut.qml
import QtQuick 2.15
import AircastDesktop 1.0

Text {
    id: root
    
    font.pixelSize: 12
    color: Theme.withAlpha(Theme.foreground, 0.6)
    horizontalAlignment: Text.AlignRight
}
